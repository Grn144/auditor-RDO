# Auditor de Relatórios - Diário de Obra
#
# CLI (baixar + nomear via linha de comando): NÃO precisa de nada além do Python.
# A API do Diário de Obra e os downloads usam a biblioteca padrão (urllib).
#
# Para a INTERFACE WEB (app.py / "Auditor RDO.bat"):
flask>=3.0.0

# Recomendados (evitam erro de SSL no Windows e melhoram a IA):
truststore>=0.9.0    # usa a store de certificados do Windows (evita erro de SSL)
certifi>=2024.0.0    # cadeia de certificados alternativa (reserva do truststore)
pillow>=10.0.0       # redimensiona as fotos antes de enviar à IA

# Só para auditoria por IA com o provedor Anthropic/Claude (pago):
anthropic>=1.0.0

# Obs.: a auditoria por IA com Groq (gratuito) NÃO precisa de pacote extra — usa urllib.

# Só para rodar os testes (pytest tests/) - não precisa em produção nem para uso local normal.
pytest>=8.0.0
